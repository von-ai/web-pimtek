'use client';
import React from 'react';

const RegisButton = () => {
  return (
    <div>
      <button className="w-auto h-auto px-[25px] py-[10px] bg-secondary-yellow font-semibold text-sm rounded-full">
        Daftar Sekarang
      </button>
    </div>
  );
};

export default RegisButton;
